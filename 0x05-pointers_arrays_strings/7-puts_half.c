#include "main.h"
/**
 * puts_half - prints half of a string
 * @str: pointer
 *
 * Return: void
 */
void puts_half(char *str)
{
	int i;
	int j;

	for (j = 0; str[j] != 0; j++)
	{

	}
	for (i = 0; i > (j / 2); i++)
	{
		str[i];
	}
}
